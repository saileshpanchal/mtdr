{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/saileshpanchal/mtdr/blob/main/records/decision/schema/dac.schema.json",
  "title": "Decision Assurance Case \u2014 frontmatter and structured assurance block",
  "description": "Schema for the YAML frontmatter of a Decision Assurance Case (DAC), per spec-decision-assurance.md \u00a75, plus the optional structured `assurance` block for adopters who want the projections machine-readable. The markdown body carries the reasoning and is deliberately unconstrained. Frontmatter is validated as JSON: normalise YAML dates to ISO-8601 strings before validation. See TDR-0009.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "tdr_id",
    "title",
    "status",
    "disposition",
    "assessed_at",
    "accountable_owner",
    "review_date",
    "confidence",
    "supersedes"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^DAC-[0-9]+$"
    },
    "conforms_to": {
      "type": "string",
      "pattern": "^mtdr/[a-z][a-z-]*/(tdr|dac|vr)@[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Durable binding to the specification this record is presented as conforming to, as `mtdr/<package>/<record>@<version>`. OPTIONAL in the schema so that every record authored before it existed still validates; the MTDR repository requires it of its own governed records and tests/verify.py enforces that. It is what lets a DETACHED record -- one file, with no repository around it -- identify its record type and resolve the specification and schema that govern it, without repository-private interpretation (DAC-0032 constraint 2, TDR-0043)."
    },
    "tdr_id": {
      "type": "string",
      "pattern": "^TDR-[0-9]+$",
      "description": "The decision under test."
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "enum": [
        "proposed",
        "accepted",
        "superseded",
        "stale"
      ],
      "description": "`stale` is the standing-loss state (spec \u00a75.1): a stale case authorises nothing."
    },
    "disposition": {
      "$ref": "#/$defs/disposition"
    },
    "assessed_at": {
      "$ref": "#/$defs/date"
    },
    "accountable_owner": {
      "type": "string",
      "minLength": 1,
      "description": "A named individual \u2014 not a committee."
    },
    "review_date": {
      "$ref": "#/$defs/date",
      "description": "When the case must be re-examined even if no trigger fires."
    },
    "confidence": {
      "$ref": "#/$defs/level",
      "description": "Confidence in the assessment itself."
    },
    "supersedes": {
      "type": "string",
      "pattern": "^(none|DAC-[0-9]+)$"
    },
    "assurance": {
      "$ref": "#/$defs/assurance"
    }
  },
  "$defs": {
    "date": {
      "type": "string",
      "format": "date",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "level": {
      "enum": [
        "low",
        "medium",
        "high"
      ]
    },
    "disposition": {
      "enum": [
        "proceed",
        "proceed-with-constraints",
        "experiment-within-bounded-authority",
        "escalate",
        "defer-pending-evidence",
        "stop"
      ],
      "description": "Design-time assurance dispositions (distinct from any runtime disposition vocabulary an adopter uses)."
    },
    "assurance": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional structured summary of the case. The body remains the reasoning; this block makes the conclusions queryable.",
      "properties": {
        "uncertainty": {
          "$ref": "#/$defs/uncertainty"
        },
        "risk_delta": {
          "$ref": "#/$defs/riskDelta"
        },
        "risk_vector": {
          "$ref": "#/$defs/riskVector"
        },
        "decision_debt": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/debtItem"
          }
        },
        "monitoring_contract": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/harmHypothesis"
          },
          "description": "The design-time authoring of the decision's monitorable contract \u2014 see spec \u00a75.2 item 9."
        },
        "learning": {
          "$ref": "#/$defs/learning"
        },
        "constraints": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Non-empty when disposition is proceed-with-constraints and this block is present. The rule itself is spec \u00a75.2 item 11; a frontmatter-only case carries its constraints in the body."
        },
        "skills_run": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Which reasoning skills ran; non-material skills are recorded as such, not silently skipped."
        }
      }
    },
    "uncertainty": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "knowns",
        "unknowns",
        "assumptions"
      ],
      "properties": {
        "knowns": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "unknowns": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "assumptions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Stated as assumptions, never as facts."
        },
        "confidence_basis": {
          "type": "string"
        }
      }
    },
    "riskDelta": {
      "type": "object",
      "additionalProperties": false,
      "description": "The case records the DELTA and its temporal shape (spec \u00a75.2 item 6); computing the accumulated and resultant organisational position at any point in time is graph work outside this standard. Declared interactions are what that graph needs.",
      "properties": {
        "increases": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "decreases": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "transfers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Risk moved, with destination: channel, cohort, process or institution."
        },
        "unknowns": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "effective_from": {
          "$ref": "#/$defs/date",
          "description": "When the declared change takes effect \u2014 not necessarily the decision date."
        },
        "expected_duration": {
          "type": "string",
          "description": "How long the declared change is expected to hold \u2014 e.g. \"12-week pilot\", \"until migration completes\", \"permanent\"."
        },
        "expected_trajectory": {
          "type": "string",
          "description": "How the delta is expected to evolve \u2014 e.g. temporary duplication during transition, decreasing after cutover, increasing as adoption scales."
        },
        "dependency_conditions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Conditions under which the declared change materialises or changes shape \u2014 another decision landing, adoption crossing a threshold, a control being retired."
        },
        "declared_interactions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Shared controls, shared identity/authority mechanisms, concentration, correlated failures, exception accumulation \u2014 the aggregation inputs (BCBS 239 lesson, applied to decisions)."
        }
      }
    },
    "riskVector": {
      "type": "object",
      "additionalProperties": false,
      "description": "All ten axes plus trend, per spec \u00a76. Never collapsed into a composite score: a single number conceals the axis that should stop the decision.",
      "required": [
        "severity",
        "likelihood",
        "exposure_volume",
        "velocity",
        "concentration",
        "detectability",
        "reversibility",
        "control_confidence",
        "cohort_distribution",
        "time_horizon",
        "trend"
      ],
      "properties": {
        "severity": {
          "$ref": "#/$defs/axis"
        },
        "likelihood": {
          "$ref": "#/$defs/axis"
        },
        "exposure_volume": {
          "$ref": "#/$defs/axis"
        },
        "velocity": {
          "$ref": "#/$defs/axis"
        },
        "concentration": {
          "$ref": "#/$defs/axis"
        },
        "detectability": {
          "$ref": "#/$defs/axis"
        },
        "reversibility": {
          "$ref": "#/$defs/axis"
        },
        "control_confidence": {
          "$ref": "#/$defs/axis"
        },
        "cohort_distribution": {
          "type": "string",
          "description": "Descriptive axis: which customer cohorts carry the exposure, and how unevenly."
        },
        "time_horizon": {
          "type": "string",
          "description": "Descriptive axis."
        },
        "trend": {
          "enum": [
            "improving",
            "deteriorating",
            "stabilising",
            "oscillating"
          ],
          "description": "Trend is often more important than today's absolute value."
        }
      }
    },
    "axis": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "level",
        "basis"
      ],
      "properties": {
        "level": {
          "$ref": "#/$defs/level"
        },
        "basis": {
          "type": "string",
          "minLength": 1,
          "description": "What the level rests on \u2014 evidence, analysis or stated judgement."
        }
      }
    },
    "debtItem": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "item",
        "kind",
        "owner",
        "due"
      ],
      "properties": {
        "item": {
          "type": "string"
        },
        "kind": {
          "enum": [
            "deferred-evidence",
            "temporary-assumption",
            "known-unknown",
            "manual-mitigation",
            "review-commitment"
          ]
        },
        "owner": {
          "type": "string",
          "description": "A named individual."
        },
        "due": {
          "$ref": "#/$defs/date"
        }
      }
    },
    "harmHypothesis": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "hypothesis",
        "counter_hypothesis",
        "test",
        "metric",
        "threshold",
        "owner",
        "trigger"
      ],
      "properties": {
        "hypothesis": {
          "type": "string"
        },
        "counter_hypothesis": {
          "type": "string",
          "description": "The harm the hypothesis could conceal \u2014 e.g. the control works but excludes."
        },
        "test": {
          "type": "string"
        },
        "metric": {
          "type": "string"
        },
        "threshold": {
          "type": "string",
          "description": "Including any maximum tolerated differential between cohorts."
        },
        "owner": {
          "type": "string"
        },
        "trigger": {
          "type": "string",
          "description": "What fires when the threshold breaches: reopen the TDR, narrow authority, suspend, redesign."
        }
      }
    },
    "learning": {
      "type": "object",
      "additionalProperties": false,
      "description": "Examined at the case's top-level review_date \u2014 no separate date field.",
      "required": [
        "hypotheses",
        "evidence_required"
      ],
      "properties": {
        "hypotheses": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "experiments": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "evidence_required": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "disposition": {
            "const": "proceed-with-constraints"
          }
        },
        "required": [
          "assurance"
        ]
      },
      "then": {
        "properties": {
          "assurance": {
            "required": [
              "constraints"
            ],
            "properties": {
              "constraints": {
                "minItems": 1
              }
            }
          }
        }
      }
    }
  ]
}
