# Package manifest — machine-readable identity and maturity. TDR-0021.
name: work
language: work
primary_record: OIR
status: candidate                # record admission test NOT passed — metadata and scope only
admission: not-passed
conformance_layers: {}           # a candidate carries none, by rule
